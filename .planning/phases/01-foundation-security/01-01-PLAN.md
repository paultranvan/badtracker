---
phase: 01-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
  - app/sign-in.tsx
  - app/(app)/_layout.tsx
  - app/(app)/(tabs)/_layout.tsx
  - app/(app)/(tabs)/index.tsx
  - app/(app)/(tabs)/settings.tsx
  - src/i18n/index.ts
  - src/i18n/locales/fr.json
  - src/i18n/locales/en.json
  - app.json
  - tsconfig.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Expo app builds and runs on Android emulator without errors"
    - "App displays in French by default"
    - "App can switch between French and English"
    - "File-based routing works with tab navigation"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Root layout with i18n initialization"
      min_lines: 15
    - path: "app/(app)/(tabs)/_layout.tsx"
      provides: "Tab navigator with home and settings tabs"
      min_lines: 10
    - path: "src/i18n/index.ts"
      provides: "i18next configuration with FR/EN"
      min_lines: 20
    - path: "src/i18n/locales/fr.json"
      provides: "French translations"
    - path: "src/i18n/locales/en.json"
      provides: "English translations"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/i18n/index.ts"
      via: "import side-effect"
      pattern: "import.*i18n"
---

<objective>
Scaffold the Expo project with file-based routing (expo-router), tab navigation, and FR/EN internationalization.

Purpose: Establish the project skeleton that all subsequent plans build on — routing structure, folder conventions, and bilingual UI support.
Output: Running Expo app with tab navigation and i18n working in French (default) and English.
</objective>

<execution_context>
@/home/paul/.claude/get-shit-done/workflows/execute-plan.md
@/home/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security/01-RESEARCH.md
@.planning/phases/01-foundation-security/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Expo project and install dependencies</name>
  <files>
    package.json
    app.json
    tsconfig.json
  </files>
  <action>
    Create a new Expo project with TypeScript template:
    ```bash
    npx create-expo-app@latest . --template blank-typescript
    ```

    If project already exists (package.json present), skip creation and just install dependencies.

    Install all Phase 1 dependencies in one batch:
    ```bash
    npx expo install expo-router expo-secure-store expo-localization expo-splash-screen @react-native-async-storage/async-storage
    npm install axios zod i18next react-i18next
    ```

    Configure app.json for expo-router:
    - Set "scheme" to "badtracker"
    - Ensure "web.bundler" is "metro"
    - Set "plugins" to include "expo-router" and "expo-secure-store"

    Configure tsconfig.json:
    - Add path alias: `"@/*": ["./src/*"]`
    - Ensure "strict": true

    French default language: Set app.json "locales" or leave default and handle in code via i18next.
  </action>
  <verify>
    ```bash
    npx expo doctor
    ```
    Should report no critical issues. package.json contains all expected dependencies.
  </verify>
  <done>
    Expo project initialized with all Phase 1 dependencies installed. TypeScript strict mode enabled. Path aliases configured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up i18n with French/English and create routing structure</name>
  <files>
    src/i18n/index.ts
    src/i18n/locales/fr.json
    src/i18n/locales/en.json
    app/_layout.tsx
    app/sign-in.tsx
    app/(app)/_layout.tsx
    app/(app)/(tabs)/_layout.tsx
    app/(app)/(tabs)/index.tsx
    app/(app)/(tabs)/settings.tsx
  </files>
  <action>
    **i18n setup (src/i18n/):**

    Create `src/i18n/index.ts`:
    - Import i18next, initReactI18next, getLocales from expo-localization
    - Configure with resources: { fr: { translation: fr }, en: { translation: en } }
    - Detect device language via `getLocales()[0]?.languageCode`
    - Default to 'fr' (French) unless device language is 'en'
    - Set fallbackLng to 'fr' (per user decision: French is default)
    - Set interpolation.escapeValue to false

    Create `src/i18n/locales/fr.json` with initial keys:
    ```json
    {
      "common": {
        "loading": "Chargement...",
        "retry": "Reessayer",
        "cancel": "Annuler",
        "save": "Enregistrer",
        "logout": "Deconnexion",
        "settings": "Parametres",
        "home": "Accueil"
      },
      "auth": {
        "title": "BadTracker",
        "subtitle": "Votre classement badminton",
        "licence": "Numero de licence",
        "password": "Mot de passe",
        "rememberMe": "Se souvenir de moi",
        "login": "Se connecter",
        "loginError": "Identifiants incorrects",
        "networkError": "Pas de connexion internet",
        "serverError": "Le serveur FFBaD est indisponible"
      },
      "settings": {
        "title": "Parametres",
        "language": "Langue",
        "french": "Francais",
        "english": "English",
        "logoutConfirm": "Voulez-vous vous deconnecter ?"
      }
    }
    ```

    Create `src/i18n/locales/en.json` with matching English translations.

    **Routing structure (app/):**

    Create `app/_layout.tsx` (root layout):
    - Import `src/i18n/index.ts` as side-effect (initializes i18n)
    - Render a Stack navigator (expo-router)
    - This will later wrap with SessionProvider (Plan 03 adds it)
    - For now, just render the Stack with (app) and sign-in screens

    Create `app/sign-in.tsx`:
    - Placeholder login screen with text "Sign In" (Plan 03 builds the full form)
    - Use useTranslation hook for "auth.title" text

    Create `app/(app)/_layout.tsx`:
    - Protected layout shell (Plan 03 adds auth guard)
    - For now, render Stack with (tabs) child

    Create `app/(app)/(tabs)/_layout.tsx`:
    - Tab navigator with two tabs: Home (index) and Settings
    - Use translated tab labels from i18n

    Create `app/(app)/(tabs)/index.tsx`:
    - Placeholder dashboard screen showing "BadTracker" title
    - Use useTranslation for text

    Create `app/(app)/(tabs)/settings.tsx`:
    - Settings screen with language toggle (FR/EN)
    - Use AsyncStorage to persist language preference
    - Language switch calls `i18next.changeLanguage('fr'|'en')`
    - Placeholder for logout button (Plan 03 adds real logout)
  </action>
  <verify>
    ```bash
    npx expo start --android
    ```
    App launches on Android. Tab navigation works between Home and Settings. Language toggle switches all visible text between French and English. French is the default language.
  </verify>
  <done>
    App has working file-based routing with tab navigation. i18n initialized with FR (default) and EN. Language toggle in settings persists preference. All UI text uses translation keys.
  </done>
</task>

</tasks>

<verification>
- `npx expo doctor` reports no issues
- App builds and runs on Android emulator
- Tab navigation works (Home, Settings)
- Default language is French
- Language toggle in Settings switches all text to English and back
- Language preference persists across app restarts (AsyncStorage)
</verification>

<success_criteria>
- Expo project scaffolded with TypeScript, strict mode, path aliases
- expo-router file-based routing functional with tab navigator
- i18next configured with FR (default) and EN
- All UI text uses translation keys (no hardcoded strings)
- Language preference persists via AsyncStorage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security/01-01-SUMMARY.md`
</output>
