---
phase: 02-player-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useDebounce.ts
  - src/hooks/usePlayerSearch.ts
  - app/(app)/(tabs)/search.tsx
  - app/(app)/(tabs)/_layout.tsx
  - src/i18n/locales/fr.json
  - src/i18n/locales/en.json
autonomous: true
requirements:
  - PLYR-01
  - PLYR-02

must_haves:
  truths:
    - "User can type a player name in a search bar and see matching results"
    - "User can type a licence number in the search bar and find the exact player"
    - "Search fires automatically after 300ms debounce with minimum 3 characters"
    - "Empty results show a 'No players found' message"
  artifacts:
    - path: "src/hooks/useDebounce.ts"
      provides: "Generic debounce hook"
      exports: ["useDebounce"]
    - path: "src/hooks/usePlayerSearch.ts"
      provides: "Search state management with auto-detect name vs licence"
      exports: ["usePlayerSearch"]
    - path: "app/(app)/(tabs)/search.tsx"
      provides: "Search screen with TextInput and FlatList results"
      min_lines: 40
    - path: "src/i18n/locales/fr.json"
      provides: "French search translations"
      contains: "search"
    - path: "src/i18n/locales/en.json"
      provides: "English search translations"
      contains: "search"
  key_links:
    - from: "app/(app)/(tabs)/search.tsx"
      to: "src/hooks/usePlayerSearch.ts"
      via: "usePlayerSearch hook"
      pattern: "usePlayerSearch"
    - from: "src/hooks/usePlayerSearch.ts"
      to: "src/api/ffbad.ts"
      via: "searchPlayersByKeywords and searchPlayersByName"
      pattern: "searchPlayers"
    - from: "src/hooks/usePlayerSearch.ts"
      to: "src/hooks/useDebounce.ts"
      via: "useDebounce(query, 300)"
      pattern: "useDebounce"
---

<objective>
Build the player search screen with live debounced search, auto-detecting name vs licence number input.

Purpose: PLYR-01 and PLYR-02 — users can find any FFBaD player by name or licence number via a single search bar with live results.
Output: Search tab with TextInput, FlatList results, debounce hook, search state hook, FR/EN translations.
</objective>

<execution_context>
@/home/paul/.claude/get-shit-done/workflows/execute-plan.md
@/home/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-player-discovery/02-RESEARCH.md
@.planning/phases/02-player-discovery/02-CONTEXT.md
@src/api/ffbad.ts
@src/api/schemas.ts
@src/api/errors.ts
@src/i18n/locales/fr.json
@src/i18n/locales/en.json
@app/(app)/(tabs)/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create debounce hook and player search hook</name>
  <files>src/hooks/useDebounce.ts, src/hooks/usePlayerSearch.ts</files>
  <action>
Create `src/hooks/useDebounce.ts`:
- Generic `useDebounce<T>(value: T, delayMs: number): T` hook
- Uses useState + useEffect with setTimeout cleanup
- Returns debounced value after delayMs of inactivity

Create `src/hooks/usePlayerSearch.ts`:
- Exports `usePlayerSearch()` returning `{ query, setQuery, results, isLoading, error, clearResults }`
- `results` type: array of objects from LicenceSearchResponse.Retour (use the Zod-inferred type)
- Uses `useDebounce(query, 300)` for the debounced query
- Guard: if `debouncedQuery.length < 3`, clear results and return early
- Auto-detect search type: if input matches `/^\d+$/` (all digits) use `searchPlayersByKeywords`, otherwise use `searchPlayersByName`
- Handle FFBaD API string Retour (no results) — set results to empty array, not error
- Handle errors from API (NetworkError, ServerError) — set error state with i18n key
- Track staleness: compare debouncedQuery at response time vs current debouncedQuery to prevent race conditions
- Use `cancelled` flag pattern (like auth/context.tsx) in useEffect cleanup to prevent state updates after unmount
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Manually verify:
- useDebounce exports correctly
- usePlayerSearch imports from api/ffbad.ts and hooks/useDebounce.ts
- Handles string Retour (empty results), array Retour (results), and errors
  </verify>
  <done>Both hooks compile, usePlayerSearch auto-detects name vs licence, debounces at 300ms, and handles all API response shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Build search screen with tab integration and i18n</name>
  <files>app/(app)/(tabs)/search.tsx, app/(app)/(tabs)/_layout.tsx, src/i18n/locales/fr.json, src/i18n/locales/en.json</files>
  <action>
Create `app/(app)/(tabs)/search.tsx`:
- Import and use `usePlayerSearch()` hook
- TextInput at top: placeholder from i18n (`search.placeholder`), `autoCorrect={false}`, `autoCapitalize="none"`, `returnKeyType="search"`, `clearButtonMode="while-editing"`
- Below TextInput: conditional rendering:
  - `isLoading` → ActivityIndicator centered
  - `error` → error message Text with retry suggestion
  - `results.length > 0` → FlatList with `keyExtractor={(item) => item.Licence}`
  - `query.length >= 3 && !isLoading && results.length === 0` → "No players found" text (per locked decision: simple message, no extra guidance)
  - `query.length < 3` → nothing or subtle hint text
- Each FlatList item (Pressable): display `{Nom} {Prenom}` as primary text, `{NomClub}` as secondary text, licence number as tertiary. Use `router.push({ pathname: '/player/[licence]', params: { licence: item.Licence } })` on press (this route will be created in plan 02).
- Style: simple white background, consistent with existing app style. Use StyleSheet.create for all styles.

Update `app/(app)/(tabs)/_layout.tsx`:
- Add a `search` tab between `index` (home) and `settings`
- Label from i18n: `t('search.tab')`

Update `src/i18n/locales/fr.json` — add `search` section:
```json
"search": {
  "tab": "Recherche",
  "placeholder": "Nom du joueur ou n° de licence",
  "noResults": "Aucun joueur trouvé",
  "error": "Erreur de recherche",
  "minChars": "Tapez au moins 3 caractères"
}
```

Update `src/i18n/locales/en.json` — add `search` section:
```json
"search": {
  "tab": "Search",
  "placeholder": "Player name or licence number",
  "noResults": "No players found",
  "error": "Search error",
  "minChars": "Type at least 3 characters"
}
```
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Run `npx expo start` and verify:
1. Search tab appears in bottom tab bar between Home and Settings
2. Search screen shows a text input with placeholder
3. Typing 3+ characters shows loading indicator then results (or "no players found")
4. Results display name, club, licence for each player
5. Tapping a result attempts navigation (will 404 until Plan 02 creates the profile route — that's expected)
  </verify>
  <done>Search tab visible in navigation. TextInput fires debounced search after 300ms and 3+ chars. Results render in FlatList with name/club/licence. Empty results show "Aucun joueur trouvé" / "No players found".</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] Search tab appears in bottom navigation
- [ ] Typing "Martin" (common French name) returns results after brief delay
- [ ] Typing a licence number returns results
- [ ] Typing fewer than 3 characters shows no results and no loading
- [ ] Empty search results show a simple "no players found" message
- [ ] Fast typing (race condition test): type quickly, only final results display
- [ ] Network error: shows error message, not crash
</verification>

<success_criteria>
Users can search for any FFBaD player by name or licence number via the search tab. Results appear live with debounce. All FR/EN strings are translated. The search hook properly auto-detects input type and handles all API response shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-player-discovery/02-01-SUMMARY.md`
</output>
